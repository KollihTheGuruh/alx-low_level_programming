#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    /* Use putchar to print "_putchar", and a newline */
	char _putchar[8] = {'_', 'p', 'u', 't', 'c', 'h', 'a', 'r'};
	unsigned int a;

	for (a = 0; sizeof(a); a++)
	{
		putchar(a[a]);
	}
	putchar('\n');
	return (0);

}
